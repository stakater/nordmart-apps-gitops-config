apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: preview
spec:
  generators:
  - git:
      repoURL: https://github.com/stakater/gitops-config-template.git
      revision: HEAD
      directories:
      - path: "01-gabbar/01-review/00-preview/"
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/stakater/gitops-config-template.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{path.basename}}'